// next controller to be added to the project is the auth part
// will use token based on server private key
// login method to be decided (passport? simple username+password?)

//const auth = require('./auth');
const errorLog = require('./errorLog');

module.exports = {
    //auth,
    errorLog
}